Important imports::

const express = require("express");

const app = express()

const router = app.Router();

const mongoose = require("mongoose")

require("dotenv").config();

const PORT = process.env.PORT;

app.use(express.json()) // needed for json parsing from req body (because node initailly blocks the express from getting the body)
--------------------------------------
npm commands::

npm init -y -> To intialize the 

npm i express

npm i nodemon -D

//Package.json
"scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "node index.js",
    "dev": "nodemon index.js"
  },

npm run test
npm run serve
npm run dev

npm i mongoose

npm install mongodb